# PostgreSQL (Railway injeta em produção)
DATABASE_URL=postgres://user:pass@host:5432/dbname

# Porta para dev; em produção Railway define automaticamente
PORT=8080

NODE_ENV=production

# Webhook secret (ex: TradingView, Kiwify…)
WEBHOOK_SECRET=seu_token_secreto

# (Opcional) CoinStats – não obrigatório para as rotas públicas
COINSTATS_API_KEY=

# Outras integrações (se usadas)
OPENAI_API_KEY=
BYBIT_API_KEY=
BYBIT_API_SECRET=
ZAPI_TOKEN=
ZAPI_INSTANCE_URL=
WHATSAPP_NUMBER=
